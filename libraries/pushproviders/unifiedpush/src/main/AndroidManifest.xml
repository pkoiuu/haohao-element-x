<?xml version="1.0" encoding="utf-8"?><!--
  ~ Copyright (c) 2025 Element Creations Ltd.
  ~ Copyright 2023 New Vector Ltd.
  ~
  ~ SPDX-License-Identifier: AGPL-3.0-only OR LicenseRef-Element-Commercial.
  ~ Please see LICENSE files in the repository root for full details.
  -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 推送前台服务所需权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- HaohaoChat v26.06.5: specialUse FGS 类型 (Android 15+ 替代 dataSync) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- HaohaoChat v26.06.4: 网络状态监听权限 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application>
        <receiver
            android:name=".VectorUnifiedPushMessagingReceiver"
            android:enabled="true"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="org.unifiedpush.android.connector.MESSAGE" />
                <action android:name="org.unifiedpush.android.connector.UNREGISTERED" />
                <action android:name="org.unifiedpush.android.connector.NEW_ENDPOINT" />
                <action android:name="org.unifiedpush.android.connector.REGISTRATION_FAILED" />
                <action android:name="org.unifiedpush.android.connector.REGISTRATION_REFUSED" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".KeepInternalDistributor"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <!--
                This action is checked to track installed and uninstalled distributors.
                We declare it to keep the background sync as an internal
                unifiedpush distributor.
                -->
                <action android:name="org.unifiedpush.android.distributor.REGISTER" />
            </intent-filter>
        </receiver>

        <!-- HaohaoChat 内置 ntfy 推送分发器 -->
        <receiver
            android:name=".EmbeddedNtfyDistributor"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="org.unifiedpush.android.distributor.REGISTER" />
                <action android:name="org.unifiedpush.android.distributor.UNREGISTER" />
            </intent-filter>
        </receiver>

        <!-- HaohaoChat 推送前台服务 — 保持 WebSocket 后台存活 -->
        <!-- HaohaoChat v26.06.5: specialUse FGS 类型，避免 Android 15+ dataSync 6h 超时 -->
        <service
            android:name=".PushForegroundService"
            android:foregroundServiceType="specialUse|dataSync"
            android:enabled="true"
            android:exported="false"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="UnifiedPush push notification delivery via persistent WebSocket connection" />
        </service>

        <!-- HaohaoChat v26.06.4: 设备重启后自动启动推送服务 -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUERY_PACKAGE_RESTART" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
